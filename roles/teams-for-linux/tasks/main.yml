- name: Instalar Team for Linux via snap
  become: true
  ansible.builtin.command: snap install teams-for-linux
  register: snap_install
  changed_when: "'installed' in snap_install.stdout"
  failed_when: snap_install.rc != 0 and 'already installed' not in snap_install.stderr